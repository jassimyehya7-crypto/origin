# Audit MVP OffresLocal — 2026-09-14

## Prêt
- 3 interfaces : `/`, `/pro` (+ cloture), `/fondateur`
- Pattern C, stock, code EC-xxxx, no-show clôture, strikes in-app
- Supabase `ec_*` + Realtime + migrations versionnées

## P0 — corrigés (CHANGELOG-P0-2026-09-14.md)
1. Auth Pro / Fondateur (PIN fail-closed)
2. Strikes → `ec_strikes` (+ fallback fichier)
3. `soft_user_id` persisté
4. API réservations filtrée
5. Annulation client
6. Service role server writes + anon lockdown migration

## Remaining for true ~9.9
Supabase Auth, IDOR-safe cancel, rate limits, apply lockdown SQL on live, atomic stock RPC.
